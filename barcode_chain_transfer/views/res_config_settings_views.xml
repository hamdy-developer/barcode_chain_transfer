<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="res_config_settings_view_form_barcode_chain" model="ir.ui.view">
        <field name="name">res.config.settings.form.barcode.chain</field>
        <field name="model">res.config.settings</field>
        <field name="priority" eval="25"/>
        <field name="inherit_id" ref="stock.res_config_settings_view_form"/>
        <field name="arch" type="xml">
            <!-- Add Approve Chain Transfers setting under the operations settings -->
            <xpath expr="//block[@name='operations_setting_container']" position="inside">
                <setting id="approve_chain_transfer_settings" string="Approve Chain Transfers" help="Require inventory manager approval before validating chain transfers.">
                    <field name="approve_chain_by_manager"/>
                </setting>
                <setting id="default_chain_transit_location_settings" string="Default Transit Location" help="Default transit location pre-filled in Barcode Chain Transfer dialog.">
                    <div class="content-group">
                        <div class="mt16">
                            <field name="company_chain_transit_location_id" class="o_light_label" options="{'no_create': True}"/>
                        </div>
                    </div>
                </setting>
            </xpath>
        </field>
    </record>
</odoo>
